export const publicApiUrl = "http://de1.api.radio-browser.info/json/"

export const routes: {
    countries: string,
    all: string,
    languages: string,
    states: string,
    codecs: string,
    tags: string
} = {
    all: "stations",
    countries: "countries",
    languages: "languages",
    states: "states",
    codecs: "codecs",
    tags: "tags"
}
